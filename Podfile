# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FordCase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
pod 'SnapKit'
pod 'Alamofire', '~> 5.4' # Ağ istekleri için
pod 'Kingfisher' # Cache'leme ve resim işlemleri için
pod 'RealmSwift' # Cache'leme için veritabanı yönetimi
  # Pods for FordCase

end
